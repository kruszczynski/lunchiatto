section.orderer
  .margin-bottom-medium.light-weight Today <strong>#{order.user.name}</strong> pays.
  .margin-bottom-medium.light-weight We're ordering from: <strong>#{order.from}</strong>
  .margin-bottom-medium.light-weight The sum of the order (with shipping) is: <strong>#{order.amount+order.shipping}</strong>
  .margin-bottom-medium.light-weight Order status is: <strong>#{order.status.humanize}</strong>
  .margin-bottom-medium= order.summary_buttons