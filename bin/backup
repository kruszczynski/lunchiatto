#!/bin/bash
# run a backup
NAME="$(date +"%Y%m%d%H%M")_dump.sql"
docker-compose run db pg_dump -U postgres -h db codequestmanager_production > /tmp/$NAME
aws s3 cp /tmp/$NAME s3://lunchiatto-backups/$NAME
