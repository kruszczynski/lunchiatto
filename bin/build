#!/bin/bash
# build lunchiatto
cd ~/lunchiatto
git pull origin master
./bin/backup
docker-compose build web_prod
docker-compose run web_prod bundle install && rake db:migrate
docker-compose up -d web_prod
