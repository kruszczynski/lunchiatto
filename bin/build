#!/bin/bash
# build lunchiatto
cd ~/lunchiatto
git pull origin master
./bin/backup
docker-compose build web_production
docker-compose up -d web_production
docker-compose run web_production rake db:migrate