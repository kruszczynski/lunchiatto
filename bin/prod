#!/bin/bash
bundle install --jobs 20 --without development test
bower install --allow-root
bundle exec rake assets:clobber
bundle exec rake assets:precompile --trace
bundle exec puma -C config/puma.rb